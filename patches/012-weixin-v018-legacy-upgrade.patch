--- a/gateway/platforms/weixin.py
+++ b/gateway/platforms/weixin.py
@@ -32,6 +32,9 @@
 from urllib.parse import quote, urlparse
 
 logger = logging.getLogger(__name__)
+
+# HERMES_WECHAT_V018_CONSOLIDATED_PATCH_V1
+# HERMES_WECHAT_V018_SOURCE_PROFILE_DISPATCH_V1
 
 WEIXIN_COPY_LINE_WIDTH = 120
 
@@ -298,6 +301,14 @@
     def get(self, account_id: str, user_id: str) -> Optional[str]:
         return self._cache.get(self._key(account_id, user_id))
 
+    def items_for_account(self, account_id: str) -> Dict[str, str]:
+        prefix = f"{account_id}:"
+        return {
+            key[len(prefix) :]: value
+            for key, value in self._cache.items()
+            if key.startswith(prefix) and isinstance(value, str) and value
+        }
+
     def set(self, account_id: str, user_id: str, token: str) -> None:
         self._cache[self._key(account_id, user_id)] = token
         self._persist(account_id)
@@ -344,47 +355,212 @@
 
 class ReplyBudgetStore:
     # WECHAT_ENHANCE_REPLY_BUDGET_COMMIT_AFTER_ACK_V2
+    # HERMES_WECHAT_CONTEXT_TOKEN_GENERATION_FENCE_V1
+    # HERMES_WECHAT_CONTEXT_BUDGET_RECONCILIATION_V1
     _TOKEN_TTL_SECONDS: float = 24 * 3600.0
     _MAX_BUBBLES_PER_TOKEN: int = 10
+
     def __init__(self, hermes_home: str):
-        self._root = _account_dir(hermes_home); self._cache: Dict[str, Dict[str, Any]] = {}
-    def _path(self, account_id: str) -> Path: return self._root / f"{account_id}.reply-budget.json"
-    def _key(self, account_id: str, user_id: str) -> str: return f"{account_id}:{user_id}"
+        self._root = _account_dir(hermes_home)
+        self._cache: Dict[str, Dict[str, Any]] = {}
+
+    def _path(self, account_id: str) -> Path:
+        return self._root / f"{account_id}.reply-budget.json"
+
+    def _key(self, account_id: str, user_id: str) -> str:
+        return f"{account_id}:{user_id}"
+
     def restore(self, account_id: str) -> None:
-        path=self._path(account_id)
-        if not path.exists(): return
-        try: data=json.loads(path.read_text(encoding="utf-8"))
-        except Exception as exc: logger.warning("weixin: failed to restore reply budget for %s: %s", _safe_id(account_id), exc); return
-        restored=0; now=time.time()
-        for uid,entry in data.items():
-            if not isinstance(entry,dict): continue
-            token=entry.get("token")
-            if not isinstance(token,str) or not token: continue
-            count=entry.get("count",0); updated_at=entry.get("updated_at",now)
-            if not isinstance(count,(int,float)) or count<0: count=0
-            self._cache[self._key(account_id,uid)]={"token":token,"count":int(count),"updated_at":float(updated_at)}; restored+=1
-        if restored: logger.info("weixin: restored %d reply budget(s) for %s", restored, _safe_id(account_id))
-    def update_token(self, account_id: str, user_id: str, token: str) -> None:
-        self._cache[self._key(account_id,user_id)]={"token":token,"count":0,"updated_at":time.time()}; self._persist(account_id)
+        path = self._path(account_id)
+        if not path.exists():
+            return
+        try:
+            data = json.loads(path.read_text(encoding="utf-8"))
+        except Exception as exc:
+            logger.warning(
+                "weixin: failed to restore reply budget for %s: %s",
+                _safe_id(account_id),
+                exc,
+            )
+            return
+        restored = 0
+        now = time.time()
+        for user_id, entry in data.items():
+            if not isinstance(entry, dict):
+                continue
+            token = entry.get("token")
+            if not isinstance(token, str):
+                continue
+            count = entry.get("count", 0)
+            updated_at = entry.get("updated_at", now)
+            generation = entry.get("generation", 1)
+            if not isinstance(count, (int, float)) or count < 0:
+                count = 0
+            if not isinstance(generation, (int, float)) or generation < 1:
+                generation = 1
+            self._cache[self._key(account_id, user_id)] = {
+                "token": token,
+                "count": int(count),
+                "updated_at": float(updated_at),
+                "generation": int(generation),
+            }
+            restored += 1
+        if restored:
+            logger.info(
+                "weixin: restored %d reply budget(s) for %s",
+                restored,
+                _safe_id(account_id),
+            )
+
+    def update_token(self, account_id: str, user_id: str, token: str) -> int:
+        key = self._key(account_id, user_id)
+        previous = self._cache.get(key) or {}
+        generation = int(previous.get("generation", 0) or 0) + 1
+        self._cache[key] = {
+            "token": token,
+            "count": 0,
+            "updated_at": time.time(),
+            "generation": generation,
+        }
+        self._persist(account_id)
+        return generation
+
+    def reconcile_from_context_tokens(
+        self,
+        account_id: str,
+        context_tokens: Dict[str, str],
+    ) -> int:
+        changed = 0
+        for user_id, token in context_tokens.items():
+            if not isinstance(token, str) or not token:
+                continue
+            key = self._key(account_id, user_id)
+            entry = self._cache.get(key)
+            if not entry or entry.get("token") != token:
+                previous_generation = int((entry or {}).get("generation", 0) or 0)
+                self._cache[key] = {
+                    "token": token,
+                    "count": 0,
+                    "updated_at": time.time(),
+                    "generation": previous_generation + 1,
+                }
+                changed += 1
+        if changed:
+            self._persist(account_id)
+            logger.info(
+                "weixin: reconciled %d context token budget(s) for %s",
+                changed,
+                _safe_id(account_id),
+            )
+        return changed
+
+    def items_for_account(self, account_id: str) -> Dict[str, Dict[str, Any]]:
+        prefix = f"{account_id}:"
+        return {
+            key[len(prefix) :]: dict(value)
+            for key, value in self._cache.items()
+            if key.startswith(prefix) and isinstance(value, dict)
+        }
+
+    def snapshot(self, account_id: str, user_id: str) -> Dict[str, Any]:
+        entry = self._cache.get(self._key(account_id, user_id))
+        if not entry:
+            return {
+                "token": "",
+                "count": 0,
+                "updated_at": 0.0,
+                "generation": 0,
+            }
+        return dict(entry)
+
     def get_valid_token(self, account_id: str, user_id: str) -> Optional[str]:
-        entry=self._cache.get(self._key(account_id,user_id))
-        if not entry: return None
-        if time.time()-entry["updated_at"]>=self._TOKEN_TTL_SECONDS: return None
-        return entry["token"]
+        entry = self._cache.get(self._key(account_id, user_id))
+        if not entry:
+            return None
+        if time.time() - float(entry.get("updated_at", 0.0)) >= self._TOKEN_TTL_SECONDS:
+            return None
+        token = entry.get("token")
+        return token if isinstance(token, str) and token else None
+
     def get_count(self, account_id: str, user_id: str) -> int:
-        entry=self._cache.get(self._key(account_id,user_id)); return int(entry.get("count",0) or 0) if entry else 0
-    def next_count(self, account_id: str, user_id: str) -> int: return self.get_count(account_id,user_id)+1
+        entry = self._cache.get(self._key(account_id, user_id))
+        return int(entry.get("count", 0) or 0) if entry else 0
+
+    def get_generation(self, account_id: str, user_id: str) -> int:
+        entry = self._cache.get(self._key(account_id, user_id))
+        return int(entry.get("generation", 0) or 0) if entry else 0
+
+    def next_count(self, account_id: str, user_id: str) -> int:
+        return self.get_count(account_id, user_id) + 1
+
     def commit_count(self, account_id: str, user_id: str, count: int) -> int:
-        key=self._key(account_id,user_id); entry=self._cache.get(key)
-        if not entry: entry={"token":"","count":0,"updated_at":time.time()}; self._cache[key]=entry
-        entry["count"]=max(int(entry.get("count",0) or 0),int(count)); entry["updated_at"]=time.time(); self._persist(account_id); return int(entry["count"])
-    def increment_and_get(self, account_id: str, user_id: str) -> int: return self.commit_count(account_id,user_id,self.next_count(account_id,user_id))
-    def remaining(self, account_id: str, user_id: str) -> int: return max(0,self._MAX_BUBBLES_PER_TOKEN-self.get_count(account_id,user_id))
-    def is_exhausted(self, account_id: str, user_id: str) -> bool: return self.get_count(account_id,user_id)>=self._MAX_BUBBLES_PER_TOKEN
+        key = self._key(account_id, user_id)
+        entry = self._cache.get(key)
+        if not entry:
+            entry = {
+                "token": "",
+                "count": 0,
+                "updated_at": time.time(),
+                "generation": 1,
+            }
+            self._cache[key] = entry
+        entry["count"] = max(int(entry.get("count", 0) or 0), int(count))
+        entry["updated_at"] = time.time()
+        self._persist(account_id)
+        return int(entry["count"])
+
+    def commit_count_if_generation(
+        self,
+        account_id: str,
+        user_id: str,
+        expected_generation: int,
+        count: int,
+    ) -> bool:
+        key = self._key(account_id, user_id)
+        entry = self._cache.get(key)
+        if not entry:
+            return False
+        if int(entry.get("generation", 0) or 0) != int(expected_generation):
+            return False
+        entry["count"] = max(int(entry.get("count", 0) or 0), int(count))
+        entry["updated_at"] = time.time()
+        self._persist(account_id)
+        return True
+
+    def increment_and_get(self, account_id: str, user_id: str) -> int:
+        return self.commit_count(
+            account_id,
+            user_id,
+            self.next_count(account_id, user_id),
+        )
+
+    def remaining(self, account_id: str, user_id: str) -> int:
+        return max(
+            0,
+            self._MAX_BUBBLES_PER_TOKEN - self.get_count(account_id, user_id),
+        )
+
+    def is_exhausted(self, account_id: str, user_id: str) -> bool:
+        return (
+            self.get_count(account_id, user_id)
+            >= self._MAX_BUBBLES_PER_TOKEN
+        )
+
     def _persist(self, account_id: str) -> None:
-        prefix=f"{account_id}:"; payload={key[len(prefix):]:value for key,value in self._cache.items() if key.startswith(prefix)}
-        try: atomic_json_write(self._path(account_id), payload)
-        except Exception as exc: logger.warning("weixin: failed to persist reply budget for %s: %s", _safe_id(account_id), exc)
+        prefix = f"{account_id}:"
+        payload = {
+            key[len(prefix) :]: value
+            for key, value in self._cache.items()
+            if key.startswith(prefix)
+        }
+        try:
+            atomic_json_write(self._path(account_id), payload)
+        except Exception as exc:
+            logger.warning(
+                "weixin: failed to persist reply budget for %s: %s",
+                _safe_id(account_id),
+                exc,
+            )
 
 
 
@@ -1280,6 +1456,8 @@
             or os.getenv("WEIXIN_SEND_CHUNK_RETRY_DELAY_SECONDS", "1.0")
         )
         self._send_text_gate = asyncio.Lock()
+        # HERMES_WECHAT_CONTEXT_DELIVERY_SERIALIZATION_V1
+        self._context_delivery_locks: Dict[str, asyncio.Lock] = {}
         self._rate_limit_circuit_threshold = max(
             1,
             int(
@@ -1365,6 +1543,14 @@
             return [str(item).strip() for item in value if str(item).strip()]
         return [str(value).strip()] if str(value).strip() else []
 
+    def _context_delivery_lock(self, chat_id: str) -> asyncio.Lock:
+        key = f"{self._account_id}:{chat_id}"
+        lock = self._context_delivery_locks.get(key)
+        if lock is None:
+            lock = asyncio.Lock()
+            self._context_delivery_locks[key] = lock
+        return lock
+
     async def connect(self, *, is_reconnect: bool = False) -> bool:
         if not check_weixin_requirements():
             message = "Weixin startup failed: aiohttp and cryptography are required"
@@ -1397,6 +1583,10 @@
         self._send_session = aiohttp.ClientSession(trust_env=True, connector=_make_ssl_connector(), timeout=_no_aiohttp_timeout)
         self._token_store.restore(self._account_id)
         self._budget_store.restore(self._account_id)
+        self._budget_store.reconcile_from_context_tokens(
+            self._account_id,
+            self._token_store.items_for_account(self._account_id),
+        )
         self._poll_task = asyncio.create_task(self._poll_loop(), name="weixin-poll")
         self._mark_connected()
         _LIVE_ADAPTERS[self._token] = self
@@ -1517,14 +1707,9 @@
         if message_id and self._dedup.is_duplicate(message_id):
             return
 
-        # Secondary content-fingerprint dedup for text messages
         item_list = message.get("item_list") or []
         text = _extract_text(item_list)
-        if text:
-            content_key = f"content:{sender_id}:{hashlib.md5(text.encode()).hexdigest()}"
-            if self._dedup.is_duplicate(content_key):
-                logger.debug("[%s] Content-dedup: skipping duplicate message from %s", self.name, sender_id)
-                return
+        is_slash_command = bool(text and text.strip().startswith("/"))
 
         chat_type, effective_chat_id = _guess_chat_type(message, self._account_id)
         if chat_type == "group":
@@ -1539,9 +1724,31 @@
 
         context_token = str(message.get("context_token") or "").strip()
         if context_token:
-            self._token_store.set(self._account_id, sender_id, context_token)
-            self._budget_store.update_token(self._account_id, sender_id, context_token)
+            async with self._context_delivery_lock(sender_id):
+                # HERMES_WECHAT_CONTEXT_TOKEN_REFRESH_BEFORE_CONTENT_DEDUP_V1
+                self._token_store.set(self._account_id, sender_id, context_token)
+                self._budget_store.update_token(
+                    self._account_id,
+                    sender_id,
+                    context_token,
+                )
         asyncio.create_task(self._maybe_fetch_typing_ticket(sender_id, context_token or None))
+
+        # HERMES_WECHAT_SLASH_COMMAND_CONTENT_DEDUP_EXEMPTION_V1
+        # Transport replay protection remains message_id based. Every accepted
+        # slash command is a new control input and must refresh context_token.
+        if text and not is_slash_command:
+            content_key = (
+                f"content:{sender_id}:"
+                f"{hashlib.md5(text.encode()).hexdigest()}"
+            )
+            if self._dedup.is_duplicate(content_key):
+                logger.debug(
+                    "[%s] Content-dedup: skipping duplicate message from %s",
+                    self.name,
+                    sender_id,
+                )
+                return
 
         # /continue: drain pending replies, never route to agent or gateway
         if text and text.strip() == "/continue":
@@ -1911,41 +2118,237 @@
                     media_result=await _deliver_media(file_path,False)
                     if media_result is not None and not getattr(media_result,"success",False): return media_result
                 except Exception as exc: logger.warning("[%s] local file delivery failed for %s: %s", self.name, file_path, exc); return SendResult(success=False,error=str(exc))
-            chunks=[c for c in self._split_text(self.format_message(final_content)) if c and c.strip()]
-            for chunk_index,chunk in enumerate(chunks):
-                chunk_metadata=dict(metadata_base); chunk_metadata["_delivery_id"]=delivery_id; chunk_metadata["_delivery_chunk_index"]=chunk_index; self._send_queue.enqueue(self._account_id, chat_id, chunk, chat_id, reply_to, chunk_metadata)
-            if chunks: return await self._drain_pending(chat_id)
-            return SendResult(success=True,message_id=None)
+            chunks = [
+                chunk
+                for chunk in self._split_text(
+                    self.format_message(final_content)
+                )
+                if chunk and chunk.strip()
+            ]
+
+            # HERMES_WECHAT_ORDINARY_REPLY_APPEND_THEN_DRAIN_V1
+            # A reply generated from an ordinary inbound message is appended
+            # behind any pre-existing pending items. The same FIFO queue is
+            # then drained from its head under the latest Context token.
+            for chunk_index, chunk in enumerate(chunks):
+                chunk_metadata = dict(metadata_base)
+                chunk_metadata["_delivery_id"] = delivery_id
+                chunk_metadata["_delivery_chunk_index"] = chunk_index
+                self._send_queue.enqueue(
+                    self._account_id,
+                    chat_id,
+                    chunk,
+                    chat_id,
+                    reply_to,
+                    chunk_metadata,
+                )
+
+            if chunks:
+                return await self._drain_pending(chat_id)
+            return SendResult(success=True, message_id=None)
         except Exception as exc: logger.error("[%s] send failed to=%s: %s", self.name, _safe_id(chat_id), exc); return SendResult(success=False,error=str(exc))
 
 
     async def _drain_pending(self, chat_id: str) -> SendResult:
         # WECHAT_ENHANCE_QUEUE_PEEK_COMMIT_V2
-        last_message_id: Optional[str]=None; sent_any=False
+        # HERMES_WECHAT_CONTEXT_DELIVERY_SERIALIZATION_V1
+        # HERMES_WECHAT_CONTEXT_TOKEN_GENERATION_FENCE_V1
+        last_message_id: Optional[str] = None
+        sent_any = False
         try:
-            while self._send_queue.has_pending(self._account_id, chat_id):
-                if self._budget_store.is_exhausted(self._account_id, chat_id):
-                    pending=self._send_queue.pending_count(self._account_id, chat_id); error=f"context_token budget exhausted; pending={pending}"; logger.info("[%s] %s for %s", self.name, error, _safe_id(chat_id)); return SendResult(success=False,error=error)
-                item=self._send_queue.peek(self._account_id, chat_id)
-                if item is None: break
-                chunk=str(item.get("content") or ""); queued_metadata=item.get("metadata"); footer_metadata=queued_metadata if isinstance(queued_metadata,dict) else {}; model_name=_footer_model_name(footer_metadata)
-                count=self._budget_store.next_count(self._account_id, chat_id); chunk_with_footer=f"{chunk}\n\n---\n\n`{count}` `{model_name}`"; context_token=self._budget_store.get_valid_token(self._account_id, chat_id) or self._token_store.get(self._account_id, chat_id)
-                delivery_id=str(item.get("delivery_id") or footer_metadata.get("_delivery_id") or "").strip()
-                try: chunk_index=int(item.get("chunk_index", footer_metadata.get("_delivery_chunk_index",0)))
-                except Exception: chunk_index=0
-                client_seed=f"{self._account_id}|{chat_id}|{delivery_id}|{chunk_index}"; client_id="hermes-weixin-"+hashlib.sha256(client_seed.encode("utf-8")).hexdigest()[:32]
-                logger.info("[%s] Weixin send attempt bubble_count=%s model=%r to=%s delivery_id_present=%s chunk_index=%s context_token_present=%s pending=%s", self.name, count, model_name, _safe_id(chat_id), bool(delivery_id), chunk_index, bool(context_token), self._send_queue.pending_count(self._account_id, chat_id))
-                await self._send_text_chunk(chat_id=chat_id, chunk=chunk_with_footer, context_token=context_token, client_id=client_id)
-                self._budget_store.commit_count(self._account_id, chat_id, count); self._send_queue.dequeue(self._account_id, chat_id)
-                logger.info("[%s] Weixin send acknowledged bubble_count=%s to=%s delivery_id_present=%s chunk_index=%s pending=%s", self.name, count, _safe_id(chat_id), bool(delivery_id), chunk_index, self._send_queue.pending_count(self._account_id, chat_id))
-                last_message_id=client_id; sent_any=True
-                if self._send_chunk_delay_seconds>0 and self._send_queue.has_pending(self._account_id, chat_id): await asyncio.sleep(self._send_chunk_delay_seconds)
-            pending=self._send_queue.pending_count(self._account_id, chat_id)
-            if pending: return SendResult(success=False,message_id=last_message_id,error=f"pending chunks remain: {pending}")
-            if sent_any: return SendResult(success=True,message_id=last_message_id)
-            return SendResult(success=True,message_id=None)
+            while True:
+                async with self._context_delivery_lock(chat_id):
+                    if not self._send_queue.has_pending(
+                        self._account_id,
+                        chat_id,
+                    ):
+                        break
+                    if self._budget_store.is_exhausted(
+                        self._account_id,
+                        chat_id,
+                    ):
+                        pending = self._send_queue.pending_count(
+                            self._account_id,
+                            chat_id,
+                        )
+                        error = (
+                            "context_token budget exhausted; "
+                            f"pending={pending}"
+                        )
+                        logger.info(
+                            "[%s] %s for %s",
+                            self.name,
+                            error,
+                            _safe_id(chat_id),
+                        )
+                        return SendResult(success=False, error=error)
+
+                    item = self._send_queue.peek(self._account_id, chat_id)
+                    if item is None:
+                        break
+
+                    snapshot = self._budget_store.snapshot(
+                        self._account_id,
+                        chat_id,
+                    )
+                    context_token = (
+                        self._budget_store.get_valid_token(
+                            self._account_id,
+                            chat_id,
+                        )
+                        or self._token_store.get(self._account_id, chat_id)
+                    )
+                    if int(snapshot.get("generation", 0) or 0) == 0:
+                        self._budget_store.update_token(
+                            self._account_id,
+                            chat_id,
+                            context_token or "",
+                        )
+                        snapshot = self._budget_store.snapshot(
+                            self._account_id,
+                            chat_id,
+                        )
+
+                    chunk = str(item.get("content") or "")
+                    queued_metadata = item.get("metadata")
+                    footer_metadata = (
+                        queued_metadata
+                        if isinstance(queued_metadata, dict)
+                        else {}
+                    )
+                    model_name = _footer_model_name(footer_metadata)
+                    count = int(snapshot.get("count", 0) or 0) + 1
+                    generation = int(snapshot.get("generation", 0) or 0)
+                    chunk_with_footer = (
+                        f"{chunk}\n\n---\n\n`{count}` `{model_name}`"
+                    )
+                    delivery_id = str(
+                        item.get("delivery_id")
+                        or footer_metadata.get("_delivery_id")
+                        or ""
+                    ).strip()
+                    try:
+                        chunk_index = int(
+                            item.get(
+                                "chunk_index",
+                                footer_metadata.get(
+                                    "_delivery_chunk_index",
+                                    0,
+                                ),
+                            )
+                        )
+                    except Exception:
+                        chunk_index = 0
+                    client_seed = (
+                        f"{self._account_id}|{chat_id}|"
+                        f"{delivery_id}|{chunk_index}"
+                    )
+                    client_id = (
+                        "hermes-weixin-"
+                        + hashlib.sha256(
+                            client_seed.encode("utf-8")
+                        ).hexdigest()[:32]
+                    )
+                    logger.info(
+                        "[%s] Weixin send attempt bubble_count=%s "
+                        "model=%r to=%s delivery_id_present=%s "
+                        "chunk_index=%s context_token_present=%s pending=%s",
+                        self.name,
+                        count,
+                        model_name,
+                        _safe_id(chat_id),
+                        bool(delivery_id),
+                        chunk_index,
+                        bool(context_token),
+                        self._send_queue.pending_count(
+                            self._account_id,
+                            chat_id,
+                        ),
+                    )
+                    await self._send_text_chunk(
+                        chat_id=chat_id,
+                        chunk=chunk_with_footer,
+                        context_token=context_token,
+                        client_id=client_id,
+                    )
+                    committed = self._budget_store.commit_count_if_generation(
+                        self._account_id,
+                        chat_id,
+                        generation,
+                        count,
+                    )
+                    self._send_queue.dequeue(self._account_id, chat_id)
+                    if not committed:
+                        logger.info(
+                            "[%s] Weixin acknowledged stale context generation "
+                            "to=%s delivery_id_present=%s chunk_index=%s",
+                            self.name,
+                            _safe_id(chat_id),
+                            bool(delivery_id),
+                            chunk_index,
+                        )
+                    else:
+                        logger.info(
+                            "[%s] Weixin send acknowledged bubble_count=%s "
+                            "to=%s delivery_id_present=%s chunk_index=%s "
+                            "pending=%s",
+                            self.name,
+                            count,
+                            _safe_id(chat_id),
+                            bool(delivery_id),
+                            chunk_index,
+                            self._send_queue.pending_count(
+                                self._account_id,
+                                chat_id,
+                            ),
+                        )
+                    last_message_id = client_id
+                    sent_any = True
+
+                if (
+                    self._send_chunk_delay_seconds > 0
+                    and self._send_queue.has_pending(
+                        self._account_id,
+                        chat_id,
+                    )
+                ):
+                    await asyncio.sleep(self._send_chunk_delay_seconds)
+                else:
+                    await asyncio.sleep(0)
+
+            pending = self._send_queue.pending_count(
+                self._account_id,
+                chat_id,
+            )
+            if pending:
+                return SendResult(
+                    success=False,
+                    message_id=last_message_id,
+                    error=f"pending chunks remain: {pending}",
+                )
+            if sent_any:
+                return SendResult(
+                    success=True,
+                    message_id=last_message_id,
+                )
+            return SendResult(success=True, message_id=None)
         except Exception as exc:
-            pending=self._send_queue.pending_count(self._account_id, chat_id); logger.error("[%s] send/drain failed to=%s pending=%s error=%s", self.name, _safe_id(chat_id), pending, exc); return SendResult(success=False,message_id=last_message_id,error=str(exc))
+            pending = self._send_queue.pending_count(
+                self._account_id,
+                chat_id,
+            )
+            logger.error(
+                "[%s] send/drain failed to=%s pending=%s error=%s",
+                self.name,
+                _safe_id(chat_id),
+                pending,
+                exc,
+            )
+            return SendResult(
+                success=False,
+                message_id=last_message_id,
+                error=str(exc),
+            )
 
     async def _ensure_typing_ticket(self, chat_id: str) -> Optional[str]:
         """Return a valid typing ticket, refreshing from getConfig if expired.
@@ -2020,6 +2423,96 @@
         except Exception as exc:
             logger.debug("[%s] typing stop failed for %s: %s", self.name, _safe_id(chat_id), exc)
 
+    async def _send_media_with_budget(
+        self,
+        chat_id: str,
+        path: str,
+        caption: Optional[str],
+        metadata: Optional[Dict[str, Any]],
+        *,
+        force_file_attachment: bool = False,
+    ) -> SendResult:
+        # HERMES_WECHAT_MEDIA_CONTEXT_BUDGET_V1
+        # Caption text and media are separate acknowledged Weixin messages.
+        if caption:
+            caption_result = await self.send(
+                chat_id=chat_id,
+                content=caption,
+                metadata=metadata,
+            )
+            if not caption_result.success:
+                return caption_result
+
+        async with self._context_delivery_lock(chat_id):
+            if self._budget_store.is_exhausted(
+                self._account_id,
+                chat_id,
+            ):
+                return SendResult(
+                    success=False,
+                    error="context_token budget exhausted; media not sent",
+                )
+            snapshot = self._budget_store.snapshot(
+                self._account_id,
+                chat_id,
+            )
+            context_token = (
+                self._budget_store.get_valid_token(
+                    self._account_id,
+                    chat_id,
+                )
+                or self._token_store.get(self._account_id, chat_id)
+            )
+            if int(snapshot.get("generation", 0) or 0) == 0:
+                self._budget_store.update_token(
+                    self._account_id,
+                    chat_id,
+                    context_token or "",
+                )
+                snapshot = self._budget_store.snapshot(
+                    self._account_id,
+                    chat_id,
+                )
+            count = int(snapshot.get("count", 0) or 0) + 1
+            generation = int(snapshot.get("generation", 0) or 0)
+            try:
+                message_id = await self._send_file(
+                    chat_id,
+                    path,
+                    "",
+                    force_file_attachment=force_file_attachment,
+                )
+            except Exception as exc:
+                logger.error(
+                    "[%s] media send failed to=%s: %s",
+                    self.name,
+                    _safe_id(chat_id),
+                    exc,
+                )
+                return SendResult(success=False, error=str(exc))
+
+            committed = self._budget_store.commit_count_if_generation(
+                self._account_id,
+                chat_id,
+                generation,
+                count,
+            )
+            if not committed:
+                logger.info(
+                    "[%s] Weixin media acknowledged under stale context "
+                    "generation to=%s",
+                    self.name,
+                    _safe_id(chat_id),
+                )
+            else:
+                logger.info(
+                    "[%s] Weixin media acknowledged bubble_count=%s to=%s",
+                    self.name,
+                    count,
+                    _safe_id(chat_id),
+                )
+            return SendResult(success=True, message_id=message_id)
+
     async def send_image(
         self,
         chat_id: str,
@@ -2072,15 +2565,15 @@
         metadata: Optional[Dict[str, Any]] = None,
         **kwargs,
     ) -> SendResult:
-        del file_name, reply_to, metadata, kwargs
+        del file_name, reply_to, kwargs
         if not self._send_session or not self._token:
             return SendResult(success=False, error="Not connected")
-        try:
-            message_id = await self._send_file(chat_id, file_path, caption or "")
-            return SendResult(success=True, message_id=message_id)
-        except Exception as exc:
-            logger.error("[%s] send_document failed to=%s: %s", self.name, _safe_id(chat_id), exc)
-            return SendResult(success=False, error=str(exc))
+        return await self._send_media_with_budget(
+            chat_id,
+            file_path,
+            caption,
+            metadata,
+        )
 
     async def send_video(
         self,
@@ -2090,14 +2583,15 @@
         reply_to: Optional[str] = None,
         metadata: Optional[Dict[str, Any]] = None,
     ) -> SendResult:
+        del reply_to
         if not self._send_session or not self._token:
             return SendResult(success=False, error="Not connected")
-        try:
-            message_id = await self._send_file(chat_id, video_path, caption or "")
-            return SendResult(success=True, message_id=message_id)
-        except Exception as exc:
-            logger.error("[%s] send_video failed to=%s: %s", self.name, _safe_id(chat_id), exc)
-            return SendResult(success=False, error=str(exc))
+        return await self._send_media_with_budget(
+            chat_id,
+            video_path,
+            caption,
+            metadata,
+        )
 
     async def send_voice(
         self,
@@ -2107,24 +2601,17 @@
         reply_to: Optional[str] = None,
         metadata: Optional[Dict[str, Any]] = None,
     ) -> SendResult:
+        del reply_to
         if not self._send_session or not self._token:
             return SendResult(success=False, error="Not connected")
-
-        # Native outbound Weixin voice bubbles are not proven-working in the
-        # upstream reference implementation. Prefer a reliable file attachment
-        # fallback so users at least receive playable audio, even for .silk.
         fallback_caption = caption or "[voice message as attachment]"
-        try:
-            message_id = await self._send_file(
-                chat_id,
-                audio_path,
-                fallback_caption,
-                force_file_attachment=True,
-            )
-            return SendResult(success=True, message_id=message_id)
-        except Exception as exc:
-            logger.error("[%s] send_voice failed to=%s: %s", self.name, _safe_id(chat_id), exc)
-            return SendResult(success=False, error=str(exc))
+        return await self._send_media_with_budget(
+            chat_id,
+            audio_path,
+            fallback_caption,
+            metadata,
+            force_file_attachment=True,
+        )
 
     async def _download_remote_media(self, url: str) -> str:
         from tools.url_safety import is_safe_url
@@ -2190,7 +2677,10 @@
             ciphertext=ciphertext,
             upload_url=upload_url,
         )
-        context_token = self._token_store.get(self._account_id, chat_id)
+        context_token = (
+            self._budget_store.get_valid_token(self._account_id, chat_id)
+            or self._token_store.get(self._account_id, chat_id)
+        )
         # The iLink API expects aes_key as base64(hex_string), not base64(raw_bytes).
         # Sending base64(raw_bytes) causes images to show as grey boxes on the
         # receiver side because the decryption key doesn't match.
